apiVersion: v1
kind: Service
metadata:
  name: main-server-service
spec:
  selector:
    app: main-server
  ports:
  - protocol: TCP
    port: 5000
    targetPort: 5000
    nodePort: 30000 # NodePort must be in the range 30000-32767
  type: NodePort
